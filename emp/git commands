
To initialize git
    -> git init

To get status of reposotory
    -> git status

To add files to git 
    -> git add . (add all files to git)
    -> git add <file_name> (all specific file)

To commit git
    -> git commit -m '<name>'

To view all logs and commits
    -> git log

To Show all git config info
    -> git config -l

To set remote repository
    -> git remote add origin <url>

To push code
    -> git push -u origin master

To pull code
    git pull = git fetch + git rebase

    -> git pull (git pull pulls down from a remote and instantly merges.)

    -> git fetch (git fetch is similar to pull but doesn't merge. i.e. it fetches remote updates ( refs and objects ) but your local stays the same (i.e. origin/master gets updated but master stays the same) .)

To get Current branch(local)
    -> git brach

To Create Branch(local)
    -> git branch test

To change branch(local)
    -> git checkout <branch_name>








